export const enum OpType {
    BIN_OPERATION = 'Operation',
}

export const enum Op {
    SHIFTL = "SHIFTL",
    SHIFTR = "SHIFTR",
    OR = "OR",
    XOR = "XOR",
    AND = "AND",
    NOT = "NOT",
    NOOP = "NOOP",
}
